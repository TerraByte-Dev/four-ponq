# <Game> — Asset List (generate-checklist)  ·  TEMPLATE — fill in

> **`SPRITE-SPEC.md` is the HOW** (style + per-asset look + sizes + filename
> convention); **this is the WHAT** — the flat by-filename checklist to batch-generate
> in one pass. Drop finished PNGs into `art/sprites/` by filename. See **`AGENTS.md`**
> for the generate workflow.
>
> **Status:** 🗂 = to generate · 🔄 = exists but regenerate · ✅ = forged + wired (skip).
> **Render:** blank = stylized `$generate2dsprite` forge (default) · 🎞 = hyper-real
> (photoreal model, skip the forge).

| key | file | size | status | render | notes |
|---|---|---|---|---|---|
| _example_ | `sprites/example.png` | 128×128 | 🗂 |  | what it is |
| _dealer_ | `sprites/dealer.png` | 512×512 | 🗂 | 🎞 | photoreal portrait — skip the forge |

<!-- TODO: replace the example rows above with this game's real assets. Delete the
     🎞 dealer row if this game has no hyper-real assets. -->
